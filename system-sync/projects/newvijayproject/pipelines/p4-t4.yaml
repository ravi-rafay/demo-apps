apiVersion: gitops.k8smgmt.io/v3
kind: Pipeline
metadata:
  name: p4-t4
  project: newvijayproject
spec:
  active: true
  sharing:
    enabled: false
  stages:
  - config:
      namespace: vijayns1
      placement:
        labels:
        - key: rafay.dev/clusterName
          value: vijaymks
      workloadTemplate: template4
    name: deploy
    type: DeployWorkloadTemplate
